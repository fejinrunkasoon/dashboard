/** Server-side media code → adapter implKey (mirrors app/domain/media-presets). */
export const MEDIA_CODE_TO_IMPL: Record<string, string> = {
  META: 'meta',
  GOOGLE: 'google',
  TIKTOK: 'tiktok',
  SNAPCHAT: 'snapchat'
}
