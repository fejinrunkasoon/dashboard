# FFJ 广告账户运营平台 — 业务使用指南（讨论稿）

> **文档目的**：给业务讲清「现在系统能做什么、怎么用、哪里需要你们拍板」，以便确认主流程是否可行；可行则继续下一阶段开发。  
> **当前阶段**：前端业务原型（Mock 数据），**不是正式上线系统**。无真实登录、无真实媒体 API、无真实 Telegram Bot、无正式数据库。  
> **建议体验**：`pnpm install` → `pnpm dev` → 浏览器打开；右上角用户菜单 → **切换身份（Mock）**。  
> **建议角色**：先用「组织管理员」跑通主链，再用「团队负责人 / 普通成员」核对权限。

---

## 一、系统是什么 / 不是什么

| 是 | 不是 |
| -- | -- |
| 管理上游渠道来的**广告账户资产** | 广告投放工具（不在系统里开广告） |
| 记录账户进公司后的**分配、使用、产品、消耗、状态、流转** | 简单把 Excel 搬上网 |
| 管理团队**账户需求**与调度 | 纯 BI 看板（看板只观察与下钻，不在总览改资产） |
| 用分析与**预警**辅助运营 | 已接真实 Meta/Google/TikTok/Telegram 的生产系统 |

**核心业务链（请业务重点确认是否符合日常）：**

```
渠道供户 → 账户入库 / 账户池 → 团队提需求并审批
→ 先匹配池并分配（能分尽分）
→ 不够才产生缺口 → 渠道下单补库 →（模拟）与渠道沟通交付
→ 确认入库进池 → 再分配 → 需求闭合
→ 消耗 / 状态异常 → 预警处理
```

**两条硬规则（当前产品已按此实现）：**

1. **必须先匹配账户池，禁止跳过直接下渠道单。**
2. **下单补库 ≠ 自动满足需求。** 入库只进账户池；必须再到调度页再次「分配」才闭合需求。

---

## 二、角色怎么用（Mock 身份）

| 角色 | 典型能做什么 | 典型不能做什么 |
| ---- | ------------ | -------------- |
| 平台 / 组织管理员 | 看全组织账户；分配；审批打款地址；改系统设置与权限 | — |
| 团队负责人 | 审批本队需求；从池分配；换户管；管本队相关连接 | 审批打款地址；改系统级配置 |
| 普通成员 | 申请账户需求；看本队 / 被授权账户 | 审批需求；直接分配；换户管 |

体验时请业务同学分别切三种身份，确认：**谁该看见什么、谁该能点什么**是否符合真实组织习惯。

---

## 三、按模块怎么用（当前已可用）

### 1. 运营总览 `/`

**回答**：现在整体怎么样、哪里有问题？

- 选日期 / 周期 → 看 KPI、消耗趋势、归属拆分、账户结构  
- 点数字 / 卡片 → 下钻到账户、渠道、团队、预警  
- 总览**只看不下改**（不在这里转移账户或改状态）

### 2. 渠道中心 `/channels`（上游）

**回答**：户从哪来、给上游多少钱、质量怎么样？

| 页面 | 业务动作 |
| ---- | -------- |
| 渠道列表 / 分析 | 按状态、媒体、异常、在用、消耗筛选；看质量对比 |
| 订单工作台 | 建单、跟进补库订单 |
| 渠道详情 | 概览 / 账户订单 / 账户 / 媒体资产 / 质量 / 财务 / 对账 / 资料 |

**常用操作：**

- 缺户且池不够 → 建**账户订单** → 用「模拟 Telegram」走询价 → 接单 → 确认交付入库  
- 财务：预付、退款、费率档位、余额阈值、资金池（规则校验已有原型）  
- 资料：提交付款地址 → 管理员审批 → 仅 **ACTIVE** 地址可用于预付  

### 3. 账户中心 `/accounts`（中游 · 调度核心）

**回答**：有什么户、从哪来、在谁手上、跑什么、怎么调度？

| 页面 | 业务动作 |
| ---- | -------- |
| 全部账户 | 筛选（可分享 URL）、导出 CSV、批量转移 / 回收 / 分配 / 停用 / 改产品 / 改户管 |
| 账户池 | 看可分配库存；**直接分配**（填团队/成员/户管/产品）或留给调度 |
| 需求调度 | **主链核心页**：选已审批需求 → 匹配池 → 分配；不够则出缺口与预警，再去渠道下单 |
| 平台连接 | 配置媒体 App 后：授权 → 发现账户 → 勾选导入（当前以 Mock 为主） |
| 批量导入 | 下载模板 → 上传 CSV → 预览 / 修异常 → 确认入库（**不自动分配**） |
| 资产 / 分析 | 看 BM/MCC 等资产；质量透视并下钻到账户列表 |
| 账户详情 | 概览、消耗、分配、产品、状态、API 数据；单户转移 / 回收 / 停用等 |

### 4. 团队中心 `/teams`（下游）

**回答**：谁需要户、谁在用、用得好不好？

| 页面 | 业务动作 |
| ---- | -------- |
| 团队列表 / 分析 | 看持有、使用率、消耗结构 |
| 跨团队需求看板 | 多团队需求一眼看完，跳去处理 |
| 团队详情 | 概览 / 账户 / 成员 / 产品表现 / **账户需求** / 质量 |

**需求主链（请业务走一遍）：**

1. 成员：团队详情 → 账户需求 → 提交申请（待审批）  
2. 负责人：审批 → `已批准`  
3. 调度人员：账户中心 → 需求调度 → 匹配池并分配  
4. 不够：产生缺口预警 → 渠道下单 → 入库进池 → **再分配** → 需求满足 / 关闭  

### 5. 预警与待办 `/alerts`

- 常见类型：团队 / 池缺口、封户、无消耗、需求逾期、API 失效、同步失败、对账差异、渠道余额不足等  
- 动作：确认（处理中）→ 解决 / 忽略 / 重新打开；可从预警跳到对应账户 / 团队 / 渠道 / 需求 / 订单  
- 右上角通知滑层与预警列表应对齐  

### 6. 系统管理 `/settings`

| 页 | 用途 |
| -- | ---- |
| 媒体平台开通 | 填 Meta / Google / TikTok / Snapchat App 凭据（正式环境才有真实意义） |
| 产品与客户 | 自家 / 外接产品与客户维护 |
| 渠道主数据 | 开通与维护渠道 |
| 组织 | 组织 / 团队 / 成员 |
| 权限 | 角色与用户能力 |
| 规则 | 阈值等（部分自动化规则仍为原型 UI） |
| 字典 / 日志 | 枚举、操作日志、同步日志 |

---

## 四、建议业务验收顺序（约 60–90 分钟）

1. 打开系统，切主题，点一遍侧栏（5 分钟）  
2. 用成员 / 负责人 / 管理员三种身份看「账户能不能看见、能不能分配」（5 分钟）  
3. **完整主链一遍**（25–35 分钟）← 最重要：需求 → 审批 → 池分配 → 缺口下单 → 模拟 TG 入库 → 再分配 → 预警处理  
4. 账户列表批量 + 账户池直接分配（10 分钟）  
5. 渠道财务 / 地址 / 对账各点一次（10 分钟）  
6. 设置里改一条组织 / 产品 / 权限数据（10 分钟）  

更细的勾选清单见仓库内 `TEST_CASES.md`。

---

## 五、需要业务人员帮忙确认的事项（请会上拍板）

以下事项**不拍板则不宜大规模下一阶段开发**（建库、接真实 API 等）。请业务逐条给「同意 / 修改意见 / 暂缓」。

### A. 主流程与规则（P0）

| # | 确认点 | 业务需回答 |
| - | ------ | ---------- |
| A1 | 「先池后单、入库不自动满足需求」是否符合真实习惯？ | 是否同意；若不同意，期望改成什么 |
| A2 | 需求必须负责人审批后才能分配，是否正确？ | 是否还有「紧急跳过审批」场景 |
| A3 | 直接分配 vs 按需求分配，两种入口是否都要保留？谁常用哪种？ | 保留 / 合并 / 只留一种 |
| A4 | 闲置回收：≤1 天人工确认、>1 天可自动回池——天数与触发人是否合理？ | 确认天数与责任人 |
| A5 | 已分配且已有消耗的需求取消后：不自动回收——是否同意？ | 同意 / 要改规则 |
| A6 | 封禁 / 受限 / 停用默认不出池再分配——是否同意？ | 同意 / 例外情况 |

### B. 角色与权限（P0）

| # | 确认点 | 业务需回答 |
| - | ------ | ---------- |
| B1 | 三角色（组织管理员 / 团队负责人 / 成员）是否够用？ | 是否要「财务专员」「户管专岗」等 |
| B2 | 成员只能看本队 + 显式授权账户，是否符合保密要求？ | 同意 / 要放宽 / 要收紧 |
| B3 | 仅管理员可审批付款地址；负责人可分配与换户管——是否合理？ | 列出例外 |
| B4 | 真实上线后的登录方式？ | SSO / 企业微信 / 账号密码 / 其他 |

### C. 主数据与字段（P0）

| # | 确认点 | 业务需提供 / 确认 |
| - | ------ | ----------------- |
| C1 | 组织、团队、成员清单与层级 | 提供一份真实结构样例（可脱敏） |
| C2 | 渠道清单、合作媒体、常用付款方式 | 样例 + 命名规范 |
| C3 | 产品：自家 / 外接怎么区分；客户字段是否够 | 产品与客户主数据样例 |
| C4 | 申请需求时必填项（媒体、数量、时区、资产类型等） | 按媒体列出必填 / 选填 |
| C5 | 账户上「户管 / 成员 / 产品 / 渠道」三套关系是否永远分开 | 同意 / 有合并诉求 |

### D. 财务与对账（P1，影响后续财务模块）

| # | 确认点 | 业务需回答 |
| - | ------ | ---------- |
| D1 | 预付只能打到已审批 ACTIVE 地址——是否硬规则？ | 是 / 否及例外 |
| D2 | 结算口径：媒体消耗 + 服务费；资金口径：打款 / 退款 / 账本——是否同意？ | 同意 / 补充口径 |
| D3 | 对账差异谁发起、谁确认、是否必须出预警？ | 责任人与流程 |
| D4 | 余额不足预警的阈值谁定、按渠道还是全局？ | 规则草案 |

### E. 预警与运营节奏（P1）

| # | 确认点 | 业务需回答 |
| - | ------ | ---------- |
| E1 | 哪些预警必须进「待办」、哪些仅通知即可？ | 分级清单 |
| E2 | 缺口预警后标准动作是「去调度分配」还是「直接建单」？ | 与 A1 对齐 |
| E3 | 交付逾期、长期无消耗等 SLA（天数） | 给出天数 |

### F. 媒体接入与补库沟通（P1，影响真实对接优先级）

| # | 确认点 | 业务需回答 |
| - | ------ | ---------- |
| F1 | 第一批真实对接媒体优先级？ | 如：Meta 先，Google / TikTok 后 |
| F2 | 渠道沟通是否必须走 Telegram？有无企业微信 / 邮件备份流程？ | 主通道 + 备份 |
| F3 | 外部订单号、部分交付、超交——真实是否都存在？ | 确认场景与字段 |
| F4 | 谁负责保管各媒体 App ID / Secret、OAuth 回调域名？ | 责任人 |

### G. 原型体验结论（会上必须产出）

请业务在会后给出明确结论（三选一即可）：

1. **主流程可行** → 进入下一阶段开发（见第六节）  
2. **主流程基本可行，但需改下列规则** → 列出修改清单后再开发  
3. **主流程不可行** → 暂停建库与真实对接，先改交互 / Domain  

---

## 六、明确的未来举动（研发侧路线）

> 前提：业务完成第五节确认，且主链书面验收通过（对应计划中的 E2E 闸门）。

| 顺序 | 阶段 | 做什么 | 业务需要配合什么 |
| ---- | ---- | ------ | ---------------- |
| 1 | 会签冻结 | 冻结信息架构、Domain、主流程规则（不建库） | 签字确认第五节结论 |
| 2 | PostgreSQL ERD + 迁移 | 正式表结构与种子数据 | 确认主数据枚举、字段中文名 |
| 3 | Server API，替换 Mock | 页面接真实后端，数据可持久 | 提供/导入真实主数据样例 |
| 4 | 真实 Telegram | 订单询价 / 交付走真实 Bot | Bot Token、渠道对话规范、外部订单号规则 |
| 5 | 真实媒体 Connector | 优先 Meta OAuth → 发现 → 导入；再 Google / TikTok 等 | App 凭据、回调域名、导入范围与去重规则 |
| 6 | 正式登录与权限 | 接 SSO / 企业账号 | 账号体系与角色映射表 |
| 7 | 财务对账加固 | 预付 / 退款 / 费率 / 对账上线规则 | 财务口径终稿与责任人 |
| 8 | 规则引擎补齐 | 闲置自动回收、余额跑道等从「原型 UI」落到真实触发 | 阈值与例外清单 |

**当前明确不做（避免误解）：**

- 不在未通过业务主链确认前建生产库  
- 不假装 Google / TikTok 已真实接线（原型里可能显示未实现 / 501）  
- 不把模拟 TG 当成正式沟通渠道  
- 总览页不增加「改资产」类操作  

---

## 七、业务讨论会建议议程（45–60 分钟）

| 时间 | 议题 | 产出 |
| ---- | ---- | ---- |
| 10 min | 系统定位 + 主链演示（成员申请 → 审批 → 池分配 → 缺口下单 → 入库再分配） | 全员理解「先池后单」 |
| 15 min | 业务同学亲自操作主链 + 三种角色权限 | 体验问题列表 |
| 15 min | 逐条过第五节 A–F | 每条：同意 / 修改 / 暂缓 |
| 10 min | 产出第六节「是否进入下一阶段」结论 + 业务责任人 | 书面结论与 owner |

---

## 八、文档与联系方式（研发侧）

| 材料 | 说明 |
| ---- | ---- |
| 本文 `USER_GUIDE_BUSINESS.md` | 业务讨论用使用指南 |
| `TEST_CASES.md` | 更细的手工验收勾选 |
| `plans/plan.md` | 产品主计划与阶段 STEP |
| `details.md` | 功能说明长文 |

演示与验收问题请带回研发；**规则类问题请业务当场拍板或指定跟进人**，避免口头「再看看」导致后续返工。

---

*版本：讨论稿 · 对应当前 Frontend Domain Prototype（Mock）。业务确认后请更新第五节结论与第六节启动日期。*
